# Core dependencies
numpy>=1.20.0
opencv-python>=4.5.0
pillow>=8.0.0
torch>=1.10.0
torchvision>=0.11.0

# UI dependencies
tk>=0.1.0

# Platform-specific dependencies - uncomment as needed

# Windows dependencies
pywin32>=300; platform_system == "Windows"

# macOS dependencies
# pyobjc-core>=7.0; platform_system == "Darwin"
# pyobjc-framework-Cocoa>=7.0; platform_system == "Darwin"
# pyobjc-framework-Quartz>=7.0; platform_system == "Darwin"

# Linux dependencies
# python-xlib>=0.30; platform_system == "Linux"

# Optional for improved OCR
pytesseract>=0.3.8

# Optional for plotting and visualization
matplotlib>=3.5.0
pandas>=1.3.0

# Testing
pytest>=6.0.0